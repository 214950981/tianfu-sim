# 2026-09-22

## TAKEOVER 只读检查（tianfu-sim 2.0）

- Branch `dev/tianfu-2.0`，HEAD `d71ef1a`（LOOPFIX02A-R1：修复 current event recurrence boundary），已 push，working tree clean。
- recurrence 测试 18/18 通过（含 R1-001/002/003）。
- 确认：recurrence candidate eligibility 存在；CHOOSE_EVENT_OPTION 不再被自身 recurrence gate 拒绝（reducer.ts `checkRecurrence:false`）；occurrence 仅在权威选择时记录一次；Director P1→P6 strict precedence 未变；CONTENT01（content01-v1.ts）尚未应用任何 cooldown/repeatBehavior 配置。
- Git 对照：SIM02=61a19de、HANDOFF01=4264029、LOOPFIX02A=4fba282、R1=d71ef1a；LOOPFIX02B1 失败试验无落盘痕迹（已撤回，一致）；TAKEOVER-FIX01 无独立 commit，其内容（cause.ref P2→P3 术语同步 + dev-status 更新）并入 017b577。
- Drift：dev-status.md lastReviewedCommit 停在 4fba282，未记录 R1；`.codex/tasks/` 尚无 LOOPFIX02B/B1 任务定义（INDEX.yaml 止于 LOOPFIX02A）。
- 结论：TAKEOVER STATUS: READY；下一步先补 LOOPFIX02B1 任务定义再执行（Cause closure / origin recurrence / NPC repeat gating / P6 可达窗口）。

## LOOPFIX02B1 收尾状态同步（PASS）

仅修改 `docs/dev-status.md`，未动其他文件，未跑测试，未开始 B2。

同步内容：

- Snapshot `lastReviewedCommit` → `d71ef1a5a8f31078cee893bccdd579c1dc71c043`（原 `4fba282` 已陈旧）。
- Completed 增加 `LOOPFIX02B1: PASS`；已完成能力增加 LOOPFIX02B1 小节（24 个 Event 带 recurrence、7 origin `maxOccurrences: 1`、17 个 `minNodesBetween`、regression 323/323、Core/Director/contracts 未改）。
- 未解决玩法问题 #3 P6 starvation 补充新发现：短周期重复已抑制，P6 仍为 0；根因是单个 Core NPC 多个 P4 Event 轮转保持 P4 非空；不改 Director strict precedence；留给 B3。
- 未解决玩法问题 #6 Build 平衡：明确继续暂缓，待 B2/B3 后重新模拟。
- Next 改为 `LOOPFIX02B2 — Cause Closure`，标注 B2 只做 Cause closure / lifecycle，NPC/P6 pacing 不并入，P6 归 B3。

## LOOPFIX02B1 — CONTENT01 Recurrence Application（PASS）

- 新建 `.codex/tasks/LOOPFIX02B1.yaml` 并注册进 `INDEX.yaml`。
- `content01-v1.ts`：新增 `Spec.cooldown` + `oncePerRun`/`spaced()`/`declared()` 辅助；24 个事件获得 recurrence：
  - 7 个 Cause origin = `maxOccurrences:1`（pei.broken-blade、jiang.herb-price、cen.shoulder-road、xie.secret-map、xu.mortal-letter、road.help、road.conflict）。
  - 17 个 = `minNodesBetween`（5 个高风险 P5 场景=5；12 个含全部 9 个 cause-linked echo Event=4）。
  - `repeatBehavior:'allow-cumulative'` 按事件级自动声明；重复 risk choice 声明 `allow-repeat-resolution`。
- `content01.v1` 的默认 Echo 判定**无 per-run 限制**（salience≥2 首节点即可成熟并 echo）——内容层收敛只能靠 echo Event 的 cooldown，不要试图靠改 maturity 限制频率（那会动 Cause 数值）。
- `recurrence-audit.ts` 修正：内部需 `registerNpcPack` + `validateContentPack` 注入 NPC pack（否则 content01 被误判 invalid）。`tools/recurrence-audit.mjs` 同时审计 minimal-pack 与 content01.v1。
- 新增 `tests/loopfix02b1.test.mjs`（13 例）+ `test:loopfix02b1` 脚本。
- 验证：B1 13/13、全量回归 323/323、typecheck OK、lint/content lint/secret scan/playability audit/director-audit/recurrence-audit 全清。
- **未解决（属 B2/B3）**：P4 仍长期占位 → P6 仍为 0。根因：单个 Core NPC 的 P4 池有 3 个事件，`minNodesBetween=4` 时轮转始终非空，strict precedence 下 P5/P6 永不被选中。B1 刻意不设更严上限以免扼杀长期 NPC 关系；需 B2 的事件级节流或 Cause closure 处理。
- 单局 70–80 节点实测：同一事件重复被压制（如 night-clinic 9 次但非连续），origin 仅种一次，occurrence 总数 ≈ 节点数（符合 exactly-once）。
