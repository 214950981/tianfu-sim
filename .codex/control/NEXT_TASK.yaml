version: 1
status: READY
attempt: 1
taskId: UI02ENTRY
title: UI02ENTRY — 2.0 Entry Flow High-Fidelity Visual Slice
taskDefinition: .codex/tasks/UI02ENTRY.yaml
sourceBranch: dev/tianfu-2.0
workBranch: wb-UI02ENTRY
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: remote-source-head-at-task-start
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: UI02R2A2
  previousStatus: ACCEPTED
  acceptedResultCommit: bb8c899d2076574d1e6114745c68248d4189f5b0
  acceptanceBasis:
    - human DevTools review confirms RUN_HOME now has coherent identity vitals priorities and action surfaces without the prior giant dead middle gap
    - four core actions remain first-screen visible and breakthrough remains the server-gated primary action
    - targeted UI fast-lane suites and structural audits are green
    - the attempted full suite reported only three nested-node EBUSY environment failures which reproduce identically on the untouched base tree; no product regression evidence
controllerDecision:
  strategy: propagate-accepted-visual-system-to-entry-flow
  rationale: the in-life visual system is now good enough to freeze as the 2.0 baseline; build the missing START MODE_SELECT DESTINY_OFFER RUN_OPENING visuals before live-session UI03 so the user can review a coherent beginning-to-life experience
controllerIntent:
  mode: UI_FAST_LANE
  goal: make the 2.0 game feel complete from launch through entering a life, using real public destiny-offer data and no client gameplay authority
  postTask: controller/user visually reviews entry flow; after visual acceptance run one final full regression once, then resume UI03 live-session wiring
notes:
  - This task intentionally uses targeted UI fast-lane verification only. Do not run full npm regression during the visual iteration unless shared gameplay/server logic is touched.
  - UI03 remains blocked. Do not wire transport or live command submission here.
  - Do not copy the old 1.0 black-neon start screen. It may serve only as evidence that the product needs stronger game presence; the 2.0 entry flow must use the accepted paper/ink/cinnabar/dark-gold language.
  - Open this task in a fresh WorkBuddy conversation and read Git as the only task memory.
