version: 1
status: READY
attempt: 1
taskId: UI02COPY
title: UI02COPY — Chinese Player-Facing Copy / Raw-Key Cleanup
taskDefinition: .codex/tasks/UI02COPY.yaml
sourceBranch: wb-UI02ENTRY
workBranch: wb-UI02COPY
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: explicit-ui02entry-result-head
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: UI02ENTRY
  previousStatus: ACCEPTED_PENDING_FINAL_UI02_MERGE
  previousResultCommit: 3da45910001246dd8336a5b632cf408d9b63207a
  acceptanceBasis:
    - human DevTools review confirms START MODE_SELECT DESTINY_OFFER RUN_OPENING all render correctly and share the accepted 2.0 visual system
    - DESTINY_OFFER uses real public server projection and entry screens remain one-screen
    - targeted UI fast-lane suites are green
  postAcceptanceFinding:
    - human review of the same preview exposes raw dev.* narrative keys in EVENT and SPECIAL_NODE
    - LIFE_ARCHIVE exposes raw history/build-fact keys
    - MODE_SELECT exposes internal capability enum names in unavailable-state helper text
controllerDecision:
  strategy: final-player-facing-copy-pass-before-full-regression
  rationale: do not lock or live-wire a visually coherent UI that still leaks developer identifiers; clean only presentation copy, then run the single deferred full regression after human acceptance
controllerIntent:
  mode: UI_FAST_LANE
  goal: make every currently reviewable 2.0 screen read like a player-facing Chinese game screen without changing any gameplay or server authority
  postTask: controller/user visually reviews all preview screens; if accepted, dispatch one final UI02 full-regression/audit task and then resume UI03
notes:
  - Open this in a fresh WorkBuddy conversation. Read Git as the only task memory.
  - Do not run full npm regression in this task. Use only the targeted tests listed in UI02COPY.yaml.
  - This is not permission to rewrite content rules or invent new story branches. It is a bounded presentation-copy cleanup for keys already exposed by the current preview fixtures.
  - UI03 remains blocked and must not be started.
