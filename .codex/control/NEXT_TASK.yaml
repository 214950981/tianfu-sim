version: 1
status: READY
attempt: 1
taskId: UI02FINAL
title: UI02FINAL — Final Regression / Merge Readiness Audit
taskDefinition: .codex/tasks/UI02FINAL.yaml
sourceBranch: wb-UI02COPY
workBranch: wb-UI02FINAL
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: explicit-ui02copy-result-head
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: UI02COPY
  previousStatus: ACCEPTED_BY_HUMAN_VISUAL_REVIEW
  previousResultCommit: 8d9931b9be00a8f221fb9f723af6d10ef8fb8cba
  acceptanceBasis:
    - START MODE_SELECT DESTINY_OFFER RUN_OPENING RUN_HOME EVENT SPECIAL_NODE and LIFE_ARCHIVE all render as coherent player-facing Chinese screens in WeChat DevTools
    - raw dev content keys and internal Capability enum names are no longer visible
    - EVENT SPECIAL_NODE and LIFE_ARCHIVE wording is readable and the accepted one-screen layout remains intact
controllerDecision:
  strategy: one-final-full-regression-before-merge
  rationale: the UI chain has completed human visual review; pay the full regression cost once now, then fast-forward the accumulated chain into dev and resume UI03
controllerIntent:
  mode: FINAL_AUDIT
  goal: prove merge readiness without adding features or changing visuals
  postTask: if PASS, controller will fast-forward dev/tianfu-2.0 to the accepted UI02 chain and dispatch UI03; do not start UI03 yourself
notes:
  - This is deliberately the one expensive regression after the UI_FAST_LANE iterations. Do not optimize it away.
  - Do not run a 600-run simulation because the accumulated UI chain changed no gameplay rule or balance code.
  - The known nested-process EBUSY sandbox issue may be classified as environment noise only with exact base reproduction plus direct-tool success, as specified in UI02FINAL.yaml.
  - Make no product changes beyond aggregate test registration for ui02copy if needed.
