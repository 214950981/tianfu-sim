version: 1
status: READY
attempt: 1
taskId: UI04C
title: UI04C — WeChat Live Client Vertical Slice
taskDefinition: .codex/tasks/UI04C.yaml
sourceBranch: dev/tianfu-2.0
workBranch: wb-UI04C
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: remote-source-head-at-task-start
verificationProfile: fast-lane
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
  runAggregateNpmTest: false
  rerunKnownSandboxBaseline: false
  run600Simulation: false
controllerReview:
  previousTask: UI04B
  previousStatus: ACCEPTED
  acceptedResultCommit: 282030872a83434fdb2cd4b2be67b36f060b3700
  acceptanceBasis:
    - wb-UI04B is a real single-commit descendant of the unchanged dispatched dev head
    - the four committed miniprogram runtime files are deterministically derived from accepted client-safe source modules
    - freshness artifact audit eleven negative controls and clean-vm CommonJS differential smoke all passed
    - no page Core Content server default-route or legacy-1.0 behavior changed
controllerDecision:
  strategy: fast-lane-large-client-vertical-slice
  rationale: UI04A/B stabilized the dependency and packaging boundaries; the next unit should deliver a visible end-to-end client interaction slice rather than another tiny infrastructure task
controllerIntent:
  mode: FAST_LANE_IMPLEMENTATION
  goal: wire a real dev-only WeChat live client from cloud RPC boundary through controller to interactive page states while keeping gameplay authority server-side
  postTask: controller reviews wb-UI04C; do not start the cloud-authority/backend task yourself
notes:
  - PROTOCOL v1.3 fast-lane applies. Do NOT run aggregate npm test or re-prove the known nested-process sandbox failures.
  - Run only the task's dedicated tests, directly affected UI03/UI04B tests, runtime/client audits, typecheck, lint, secret scan, and route guard.
  - Reusable tooling/environment findings go to LAST_RESULT.toolingObservations ONLY when genuinely new. A different HTTPS_PROXY port or another successful use of an existing workaround is not a finding.
  - This task should be materially larger than UI04A/B: complete the client vertical slice before stopping.
  - The new v2-live page is dev-only and registered last. Do not replace the existing start page or tabBar.
  - Do not implement or deploy the 2.0 cloudfunction/backend in this task. The fake callFunction is test injection only; production page code must target the documented RPC operation names.
  - If the client slice requires gameplay/server semantic changes, stop BLOCKED rather than pulling authority into the client.
