version: 1
status: READY
attempt: 1
taskId: UI04B
title: UI04B — WeChat Client Runtime Artifact / Packager Bridge
taskDefinition: .codex/tasks/UI04B.yaml
sourceBranch: dev/tianfu-2.0
workBranch: wb-UI04B
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: remote-source-head-at-task-start
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: UI04A
  previousStatus: ACCEPTED
  acceptedResultCommit: d75201b87a9108a2671d91a52f8be6ff75145da9
  acceptanceBasis:
    - wb-UI04A is a real single-commit descendant of the unchanged dispatched dev head
    - command-wire is the single dependency-free owner of command/envelope validation and canonical serialization while Core is only a compatibility re-export
    - application-ui and wechat-shell runtime closures no longer reach Core Content or server and the audit has a real forbidden-edge negative control
    - canonical bytes and validation/error behaviour match the untouched base and UI03 retry restore STATE_CONFLICT semantics remain green
    - aggregate regression added exactly 16 UI04A tests and had only the same three nested-process sandbox failures re-proven on untouched base with direct tools green
controllerDecision:
  strategy: prove-WeChat-packaging-boundary-before-live-transport
  rationale: miniprogramRoot is miniprogram/ while the accepted controller sources are external TypeScript packages; produce a deterministic in-root client runtime artifact before adding network/backend complexity
controllerIntent:
  mode: STANDARD_IMPLEMENTATION
  goal: make the accepted UI03/UI04A client controller physically loadable by the WeChat package without duplicating logic or importing gameplay authority
  postTask: controller reviews wb-UI04B; do not start UI04C or any live transport/cloud/page session work yourself
notes:
  - Read .codex/control/TOOLING_RUNBOOK.md before execution.
  - Reusable tooling/environment findings must be committed to LAST_RESULT.toolingObservations; chat-only observations are not durable evidence.
  - Do not hand-copy the controller or command codec into miniprogram. The artifact must be generated from the accepted source modules.
  - Do not add esbuild webpack rollup or another new bundler dependency just for this task. Use existing TypeScript/dev tooling or a deterministic repository-local generation approach.
  - A static literal require from v2-preview is allowed only as a side-effect-free packager visibility seam; the page must remain fixture-driven and visually unchanged.
  - No wx.request, 2.0 wx.cloud.callFunction, cloud persistence, deployment, default-route switch or production session wiring in UI04B.
  - If making a WeChat-loadable artifact requires gameplay or server semantic changes, stop BLOCKED and report the exact packaging conflict.
