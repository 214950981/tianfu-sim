version: 1
status: READY
attempt: 1
taskId: UI04D
title: UI04D — WeChat Cloud Authority Backend Vertical Slice
taskDefinition: .codex/tasks/UI04D.yaml
sourceBranch: dev/tianfu-2.0
workBranch: wb-UI04D
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: remote-source-head-at-task-start
verificationProfile: fast-lane
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
  runAggregateNpmTest: false
  rerunKnownSandboxBaseline: false
  run600Simulation: false
controllerReview:
  previousTask: UI04C
  previousStatus: ACCEPTED
  acceptedResultCommit: badb88e3a49cba239e93e0c6dd43fa96479bffa3
  acceptanceBasis:
    - wb-UI04C is a real single-commit descendant of the dispatched dev head
    - dedicated UI04C 18/18 proves the injected cloud boundary through real CommandGateway and the committed v2-live page harness
    - RPC parsing is fail-closed and START_RUN is derived only from the authoritative current destiny offer
    - runtime freshness audit smoke route guard typecheck lint secret scan and directly affected UI suites are green
    - no Core Content server semantics default route tabBar or legacy-1.0 page changed
controllerDecision:
  strategy: fast-lane-real-cloud-authority-backend
  rationale: the client vertical slice is complete; the largest remaining blocker to real DevTools play is a persistent authenticated authoritative cloud backend, so implement the whole backend slice rather than splitting store/function/auth/artifact into separate hour-long tasks
controllerIntent:
  mode: FAST_LANE_IMPLEMENTATION
  goal: make the dev-only v2-live client usable against a real deployable tianfu2 cloudfunction with authoritative identity persistent transactional command settlement and idempotent bootstrap
  postTask: controller reviews wb-UI04D; do not start UI04E/UI04FINAL yourself
notes:
  - PROTOCOL v1.3 fast-lane applies. Do NOT run aggregate npm test, 600-run simulation, or re-prove known nested-process sandbox failures.
  - Run the UI04D dedicated suite and directly affected server-gateway destiny-offer UI04C/UI04B suites plus cloud runtime audits, context-loader, typecheck, lint, secret scan and content01 lint.
  - The Controller fixed context.mjs to understand verificationProfile in this dispatch commit; use node .codex/context.mjs UI04D normally.
  - The cloud host must trust only getWXContext().OPENID. Do not accept a playerId or OPENID from event data as authority.
  - createRunOffer must be retry-safe. Add and persist a bootstrapId from the client; same trusted identity plus bootstrapId must recover the same run rather than manufacture another run after a timeout.
  - Use deterministic document ids on the transactional path. CloudBase document transactions support server-side transaction doc operations; do not design a where-query-dependent transaction path.
  - Preserve CommandGateway semantics. Infrastructure refactoring is allowed; gameplay/reducer/content semantics are not.
  - A wx-server-sdk dependency is allowed only inside cloudfunctions/tianfu2. Do not leak it into root/client runtime dependencies.
  - Do not deploy to the real cloud environment in this task. Produce a deployable artifact and exact manual deployment/setup instructions.
  - Record only genuinely new tooling observations.
