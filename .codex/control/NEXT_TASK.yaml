version: 1
status: READY
attempt: 2
taskId: UI02R2A2
title: UI02R2A2 — RUN_HOME Visual Recomposition / Fast UI Iteration
taskDefinition: .codex/tasks/UI02R2A2.yaml
sourceBranch: wb-UI02R2
workBranch: wb-UI02R2A2
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: explicit-ui02r2-result-head
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: UI02R2
  previousStatus: TECHNICAL_PASS_VISUAL_REJECT
  previousResultCommit: b5b2ab2e14b22c5b8ff847a369a65abdaee112d4
  userVisualFinding:
    - current RUN_HOME is functional and one-screen, but still visually too plain and document-like
    - button hierarchy and grouping improved but still do not feel polished enough for a released mini-game
    - tall-screen composition leaves too much dead space between information and the action area
    - the legacy 1.0 start screen feels substantially more game-like; the 2.0 START -> MODE_SELECT -> DESTINY_OFFER -> RUN_OPENING visual flow has not yet been implemented
controllerDecision:
  strategy: one-more-focused-home-visual-pass-before-propagating-style
  rationale: lock a convincing 2.0 visual system on RUN_HOME first, then reuse it for the still-unbuilt start flow; do not multiply an unsatisfactory style across five pages
controllerIntent:
  mode: UI_FAST_LANE
  goal: produce a materially more game-like, comfortable RUN_HOME without touching authority/gameplay and without paying a full-repo regression cost for every visual iteration
  postTask: controller/user performs immediate DevTools visual review; if accepted, run one final full regression before merging the accumulated UI02 chain, then schedule the 2.0 entry-flow visual task and only afterwards resume UI03 live wiring
notes:
  - Full npm regression is intentionally NOT required for this presentation-only iteration unless shared/non-preview code is touched.
  - Use the required targeted UI tests/audits in UI02R2A2.yaml. The final full suite is deferred until human visual acceptance, once, before merge.
  - Do not implement START/MODE_SELECT/DESTINY_OFFER/RUN_OPENING in this task. Those pages exist in the UI contract but high-fidelity 2.0 visuals have not been built yet.
  - Open this task in a fresh WorkBuddy conversation. Do not rely on the previous long UI repair chat.
