version: 1
status: READY
attempt: 1
taskId: UI04A
title: UI04A — Client-Safe Command Wire Boundary
taskDefinition: .codex/tasks/UI04A.yaml
sourceBranch: dev/tianfu-2.0
workBranch: wb-UI04A
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: remote-source-head-at-task-start
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: UI03
  previousStatus: ACCEPTED
  acceptedResultCommit: 09213468c2de5fa7c74b026b6fb45fa40154872a
  acceptanceBasis:
    - wb-UI03 is a real two-commit descendant of the unchanged dispatched dev head
    - dedicated UI03 18/18 and all required UI/UI gateway reducer gates passed
    - aggregate regression added exactly the 18 UI03 tests and had only the same three EBUSY failures proven on untouched base with direct tools green
    - WeChatRunController keeps gameplay authority server-side and reuses CommandSubmissionController for envelope retry and reconfirmation semantics
    - the additive authoritative CurrentInteraction.eventId projection is necessary for CHOOSE_EVENT_OPTION and has been aligned into viewmodel.ref by the Controller
controllerDecision:
  strategy: harden-client-runtime-boundary-before-page-wiring
  rationale: UI03 is accepted, but the inherited CommandSubmissionController runtime import through the Core barrel is not an acceptable packaging boundary for a real miniprogram client; extract the wire codec first, then wire pages and transport in UI04B
controllerIntent:
  mode: STANDARD_IMPLEMENTATION
  goal: make the UI03 controller dependency closure client-safe without changing any gameplay or command semantics
  postTask: controller reviews wb-UI04A; do not start UI04B or any page/transport work yourself
notes:
  - Read .codex/control/TOOLING_RUNBOOK.md before execution.
  - Reusable tooling/environment findings must be committed to LAST_RESULT.toolingObservations; chat-only observations are not durable evidence.
  - Do not solve this by copying a second command validator into the client. There must remain one protocol definition/validation source of truth.
  - Do not solve this merely by pointing imports at packages/core/src/command.ts if that still leaves the production client runtime coupled to gameplay Core. The accepted end state is a lower-level client-safe wire boundary outside gameplay Core, with compatibility maintained for Core/server callers.
  - No miniprogram page wiring, wx.request, cloudfunction deployment or default-route change in UI04A.
  - If a clean acyclic extraction would require gameplay semantic changes, stop BLOCKED and report the architecture conflict instead of widening scope.
