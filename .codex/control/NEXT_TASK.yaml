version: 1
status: READY
attempt: 1
taskId: UI02R2
title: UI02R2 — RUN_HOME Visual Polish / Layout Refinement
taskDefinition: .codex/tasks/UI02R2.yaml
sourceBranch: dev/tianfu-2.0
workBranch: wb-UI02R2
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: ui02r1-result-head-at-task-start
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: UI02R1
  previousStatus: PASS_ON_WORK_BRANCH_PENDING_ACCEPTANCE
  previousResultCommit: 9ecb7d1c3ef61725d624c80e6a2f136fcf1b7e16
  postAcceptanceFinding:
    - user visual review accepts the one-screen structure but finds the presentation still reads like a form/table rather than a refined game interface
    - buttons feel stiff and their hierarchy is unclear; the breakthrough CTA does not read as the primary control versus the four core actions
    - the attention region reads as a plain list instead of a "current priorities" panel; section rhythm and breathing room are weak
    - raw structural enum ids (realm ids, build stages, condition kinds, affinity/trust semantics, risk tiers) render as English keys, which breaks the Chinese presentation without changing gameplay
controllerDecision:
  strategy: polish-before-live-wiring
  rationale: with UI02R1 the information architecture, viewport matrix and safe-area contract are accepted as structure; refine presentation quality and Chinese-facing labels now, before UI03 binds live interactions, so controller wiring lands on a visually stable surface
  baseChain: dc5675d6 (remote dev/tianfu-2.0 HEAD at dispatch) -> f091c82 (UI02) -> UI02R1 result commits -> 9ecb7d1 (UI02R1 result HEAD, PASS, pending controller acceptance)
  baseCommit: 9ecb7d1c3ef61725d624c80e6a2f136fcf1b7e16
controllerIntent:
  mode: presentation-only-polish
  goal: make RUN_HOME feel like a refined small-game interface (hierarchy, breathing room, button grouping, primary CTA, attention panel) and stop rendering raw structural enum ids as English, while preserving every UI02R1 structural contract
  hardConstraints:
    - no Core / Content / Director / server projection / shell intent / gameplay rule change
    - no change to the one-screen contract, viewport matrix, safe-area policy, or scroll ownership boundaries
    - touch targets stay at or above approximately 44 CSS px; readability floor 20rpx unchanged
    - detail stays summarized on RUN_HOME with read-only drawers; no detail expansion back onto the home surface
    - presentation labels mirror contract enums only; unknown values fall back verbatim; no narrative content keys (titleKey/bodyKey/labelKey/summaryKey prose) are translated; no invented lore or fake gameplay
    - UI03 remains blocked and must not be started
  postTask: controller reviews tree diff and LAST_RESULT, then visually inspects in DevTools; UI03 stays blocked until UI02R2 is accepted
notes:
  - Read .codex/tasks/UI02R2.yaml and docs/UI02_PREVIEW.md first. docs/UI02R1_RESPONSIVE_LAYOUT.md is the controller-owned one-screen contract and must not be edited.
  - UI02R2 builds on the UI02R1 result branch head because UI02R1 is PASS but not yet accepted onto dev/tianfu-2.0. If the controller instead accepts UI02R1 onto source and moves this base, re-dispatch with an updated baseCommit.
  - The UI02R1 leak-sample test lists "mortal" as a forbidden inlined fixture value; the presentation label table now legitimately contains the six contract realm ids as keys. Update that sample list to per-run fixture values and add a dedicated UI02R2 test pinning exactly which contract enum keys may appear.
  - The dev overlay and debug floating trigger may remain; they must stay outside the product layout flow.
