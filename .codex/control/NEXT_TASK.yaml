version: 1
status: READY
attempt: 1
taskId: UI03
title: UI03 — WeChat 2.0 Live Session Controller / E2E Wiring
taskDefinition: .codex/tasks/UI03.yaml
sourceBranch: dev/tianfu-2.0
workBranch: wb-UI03
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: remote-source-head-at-task-start
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: UI02FINAL
  previousStatus: ACCEPTED
  acceptedResultCommit: 1ecc9c9c1515c1dabe1eef784f7cd7e9fae6ec82
  acceptanceBasis:
    - UI02FINAL task contract satisfied
    - dev/tianfu-2.0 was a real ancestor of wb-UI02FINAL and fast-forwarded without force
    - aggregate regression had only three task-permitted EBUSY environment failures proven on untouched base with underlying direct tools green
    - all dedicated UI suites and required gates passed
    - no gameplay Core Content Director server legacy-1.0 or default-route change entered through the UI chain
controllerDecision:
  strategy: start-live-session-controller-wiring
  rationale: the visual/UI chain is accepted and merge-ready; the highest-value next step is authoritative live session wiring without moving gameplay authority client-side
controllerIntent:
  mode: STANDARD_IMPLEMENTATION
  goal: connect the accepted 2.0 presentation layer to authoritative transport/session orchestration with real E2E evidence
  postTask: controller reviews wb-UI03; do not start UI04 or any follow-up task yourself
notes:
  - Read .codex/control/TOOLING_RUNBOOK.md before execution.
  - Any reusable tooling/environment finding must be committed to Git in LAST_RESULT.toolingObservations. Do not leave important operational knowledge only in chat.
  - Plain git push may hang in this sandbox because the user gitconfig resets credential helpers. Follow the validated push guidance in TOOLING_RUNBOOK.md and read the current proxy from HTTPS_PROXY instead of hard-coding a port.
  - WeChat DevTools can pollute the worktree; verify and isolate those artifacts before trusting digest-pinned tests.
  - Known nested-node EBUSY may be called environment noise only with the same proof standard used by UI02FINAL: untouched-base reproduction plus direct-tool success.
  - UI03 may not widen into UI04, default-route replacement, production cloud deployment, or gameplay-rule work.
