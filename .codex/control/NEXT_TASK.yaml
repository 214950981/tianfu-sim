version: 1
status: READY
attempt: 1
taskId: SIM02
title: SIM02 — Post-LOOPFIX 600-Run Balance / Reachability Verification
taskDefinition: .codex/tasks/SIM02.yaml
sourceBranch: dev/tianfu-2.0
workBranch: wb-SIM02
worktreeRequired: true
allowSyntheticTreeBase: true
basePolicy: remote-source-head-at-task-start
execution:
  readProtocolFirst: true
  commitTaskResult: true
  pushWorkBranch: true
  updateSourceBranch: false
  startNextTask: false
controllerReview:
  previousTask: LOOPFIX02B3
  previousAttempt: 2
  previousStatus: ACCEPTED
  acceptedResultCommit: fcb7a106f497c5e6b9cc2fd4ae8bdadf2d44e2ba
  acceptanceBasis:
    - P4 repeat gate now reads only the immediately previous DirectorSceneRecord.actorIds
    - P3 Director scene records persist exact actorIds from the selected CauseInstance.actorIdsByRole
    - no content/role re-derivation heuristic remains
    - same-role sibling regression coverage added
    - npm test 249/249 and all reported gates green
controllerIntent:
  mode: verification-only
  goal: run the deferred 600-run simulation and establish empirical P3/P4/P5/P6 reachability and post-LOOPFIX stability before PLAYCHECK02
  postTask: do not start PLAYCHECK02; controller will review SIM02 first
notes:
  - LOOPFIX02B3 attempt 2 is accepted. Do not reimplement or tune B3.
  - This task is evidence collection, not balance tuning.
  - Run exactly 600 deterministic runs through the established content-sim path and report reproducible configuration plus aggregate metrics.
  - P6 must be empirically observed. Same-core-NPC consecutive P4 must remain absent. Cause echo/closure must remain healthy.
  - If acceptance requires gameplay/rule/balance changes, stop BLOCKED with evidence rather than modifying them.
