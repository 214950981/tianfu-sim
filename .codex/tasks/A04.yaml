id: A04
title: command-contract
read: [.codex/contracts/command.ref,.codex/contracts/state.ref]
blockers: []
in:
  - implement GameCommand + CommandEnvelope strict schemas
  - strict unknown-field policy for authoritative commands
  - stable error codes in command.ref
  - validation only; no gameplay execution
out: [server_gateway,reducer_behavior,DB,UI]
accept:
  - client gold/realm/rank/finalState cannot enter authoritative command
  - START_RUN requires offerId+destinyId
  - invalid/unknown fields consume no RNG/time and mutate no state
  - deterministic serialization
tests: [CMD-001,CMD-002,CMD-007,CMD-008]
