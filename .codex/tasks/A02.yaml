id: A02
title: seeded-rng-numeric
read: [.codex/contracts/rng.ref,.codex/contracts/numeric.ref]
blockers: []
in:
  - implement xoshiro128ss-v1 seeding + named independent streams exactly per rng.ref
  - implement traceable per-stream drawIndex and unbiased integer helper
  - implement shared integer/bps numeric helpers
out: [event_selection,gameplay_tuning,UI,state_hash]
accept:
  - same seed+stream sequence identical across 100 runs
  - draw in one stream never perturbs another
  - all-zero seed state handled per contract
  - no Math.random/Date.now rule randomness in new Core
  - numeric helpers reject unsafe/invalid inputs
tests: [DET-001,DET-005,stream_isolation,golden_rng,numeric_boundaries]
