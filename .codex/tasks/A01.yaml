id: A01
title: foundation
read: []
blockers: []
inspect_first: [package.json,project.config.json,existing_test_or_build_config]
in:
  - create minimal packages/core, packages/content, packages/platform-contract, server, tools, tests boundaries
  - add minimal TypeScript + test runner for new 2.0 code; avoid heavy monorepo tooling
  - static dependency check: Core cannot import wx/tt/db/http/fetch/AI/platform
  - add scripts for typecheck/test/lint if absent
out: [gameplay,UI,data_migration,RNG,State,AI,Douyin]
accept:
  - existing 1.0 behavior unchanged
  - trivial pure Core test passes
  - forbidden-import fixture fails and clean tree passes
  - no secrets
  - foundation-only diff
tests: [lint_or_equivalent,typecheck,core_smoke,import_boundary,secret_scan]
