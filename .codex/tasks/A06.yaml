id: A06
title: content-registry
read: [.codex/contracts/event.ref]
in:
  - implement ContentManifest {contentVersion,rulesCompatibility,packs[],generatedAt}
  - stable IDs + pack checksum + registry lookup
  - runtime/build validation framework and content-lint CLI
  - dev-fixtures content pack
out: [60-100 events,real NPCs/builds,AI text]
accept:
  - duplicate IDs/references/version mismatch fail lint
  - manifest checksum stable for unchanged canonical content
  - old Run can request locked contentVersion
  - content cannot execute arbitrary code
security: [no_eval,no_new_function,whitelist_DSL]
tests: [CNT-001,CNT-002,CNT-005,CNT-010,manifest_determinism]
