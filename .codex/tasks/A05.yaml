id: A05
title: reducer-offered-start
read: [.codex/contracts/reducer.ref,.codex/contracts/state.ref,.codex/contracts/command.ref,.codex/contracts/lifecycle.ref,.codex/contracts/numeric.ref]
blockers: []
in:
  - implement reducer as sole mutation entry + pure createOfferedRun fixture factory
  - implement START_RUN from offered state using supplied valid 3-destiny fixture; A07 owns content generation
  - implement stateVersion success semantics + invariant assertion
  - implement reusable time/lifespan phase helpers without full event system
out: [full_destiny,event,cause,DB,UI,combat]
accept:
  - same input => canonical-equivalent output
  - input not mutated
  - invalid/failure leaves stateVersion/RNG/time unchanged
  - START_RUN cannot select outside offer or reuse consumed offer
  - age never exceeds maxAge
  - Core no IO/platform/AI
tests: [CORE-INV-06,CORE-INV-08,reducer_purity,start_run_golden,time_boundary,property_current_scope]
